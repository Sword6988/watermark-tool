# 依赖（本机已全部安装，无需 pip install 即可运行）
# 运行环境：系统 Python（必须含 tkinter；托管 Python 不带 tkinter，跑不了 GUI）
#
# 版本写法：``>=`` 是实测可用的下限，注释里的「构建机」是**冻结 exe 时实际锁定**的
# 版本 —— 打包产物对 PyMuPDF / PyInstaller 的版本敏感，复现问题请以构建机版本为准。

Pillow>=10.0        # 图片读写、文字渲染、旋转、alpha 合成（构建机 12.3.0）
PyMuPDF>=1.24.3     # PDF 读写与水印层插入（import pymupdf as fitz；构建机 1.28.2）
numpy>=1.24         # 测试中的像素判定（构建机 2.5.3）
tkinterdnd2>=0.4.2  # 拖拽投放（构建机 0.4.3；缺失时功能降级，不致命）
PyInstaller>=6.0    # **仅构建期**需要，终端用户不必装（构建机 6.22.3）

# tkinter —— 标准库，pip 装不上：装 Python 时勾选 "tcl/tk and IDLE"，或用官方安装包。
